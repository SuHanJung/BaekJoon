#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main() {
    int a = 0, b = 0;
    char w[101] = { 0 };
    scanf("%s", &w );
    for (; w[a] != 0; a++);
    a--;
    for (int i = 0; i < a - i; i++) {
        if (w[i] == w[a - i]);
        else
        {
            printf("0");
            return 0;
        }
}
    printf("1");

    return 0;
}