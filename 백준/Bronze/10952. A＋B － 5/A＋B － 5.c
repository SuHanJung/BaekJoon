#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main() {
	int a = 1, b = 1;
	
	for (int i = 1; i = 55;) {
		scanf("%d %d", &a, &b);
		if (a != 0) {
			printf("%d\n", a + b);
		}
		else
			if (b != 0) {
				printf("%d\n", a + b);
			}
			else break;
	}

	return 0;
}